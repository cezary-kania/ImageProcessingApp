<Window x:Class="APO_v1.ImageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:APO_v1"
        mc:Ignorable="d"
        Title="ImageWindow" Height="20" Closing="Window_Closing">
    <Grid>
        <Menu DockPanel.Dock="Top" Height="20" VerticalAlignment="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Save" />
                <MenuItem Header="_Save As..." />
                <Separator />
                <MenuItem Header="_Close" Click="CloseBtn_Click" />
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="_Histogram" Click="HistogramBtnClick" />
            </MenuItem>
        </Menu>
        <Image x:Name="image" Margin="0,20,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" />

    </Grid>
</Window>
